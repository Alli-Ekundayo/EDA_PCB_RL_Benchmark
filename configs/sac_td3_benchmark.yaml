board_width: 32
board_height: 32
resolution_mm: 0.5
num_comp_classes: 4
use_ratsnest: true
use_criticality: true
gat_embed_dim: 128
spatial_embed_dim: 128
fused_dim: 256
gat_heads: 4
n_envs: 8
n_steps: 256
n_epochs: 4
batch_size: 64
clip_range: 0.2
lr: 0.0003
gamma: 0.99
gae_lambda: 0.95
ent_coef: 0.02
vf_coef: 0.5
max_grad_norm: 0.5
component_rotations: 4
total_timesteps: 200000
eval_freq: 10000000
checkpoint_freq: 10000
board_dir: data/boards/rl_pcb/base_opt/training.pcb
checkpoint_dir: runs/checkpoints
seed: 42
hpwl_weight: 2.0
drc_penalty: 2.0
routability_weight: 6.0
